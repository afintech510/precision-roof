---
// Renders one or more JSON-LD blocks. Pass a single schema object or an array.
interface Props { schema: unknown }
const { schema } = Astro.props;
const items: unknown[] = Array.isArray(schema) ? schema : [schema];
---
{items.map((item) => <script type="application/ld+json" set:html={JSON.stringify(item)} />)}
